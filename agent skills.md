agent skills is like a AOP in Java Spring framework.

A skill is MD file that describes the name of the skill , and its description when should execute and how will it do.

A skill can Bundle all of the scripts code ,doc etc the agents need

You need obey the specification of the agent skills that specify some metadata ( name ,description,etc) ,and  the body (instruct the agent how to do),and the resources the agent may use (scripts code tool…) ,there some validation tool to validate your format of the agents skills specification.